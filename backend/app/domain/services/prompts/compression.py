# 记忆压缩服务相关提示词

PLANNER_COMPRESSION_PROMPT_TEMPLATE = """你是一个任务分析专家，需要压缩用户任务描述，确保AI规划器能够生成正确的JSON响应。

规划器的响应格式要求（必须严格遵守）：
```json
{{
    "message": "对用户消息的回应和思考",
    "goal": "计划目标描述", 
    "title": "计划标题",
    "steps": [
        {{
            "id": "1",
            "description": "步骤1描述"
        }},
        {{
            "id": "2", 
            "description": "步骤2描述"
        }}
    ]
}}
```

用户原始输入：
{user_content}

压缩要求：
1. 必须保留清晰的任务目标，让规划器能写出准确的goal和title
2. 必须保留足够的技术细节和操作要求，让规划器能分解出具体的可执行步骤
3. 保留所有关键约束条件、技术要求、平台限制等
4. 保留成功标准和验收条件
5. 保持任务的完整性和可执行性
6. 确保压缩后的内容能让规划器理解需要做什么、怎么做、做到什么程度

输出：请将用户任务压缩为{target_tokens}个token以内的清晰任务描述，确保规划器能够理解并生成包含具体步骤的JSON计划。必须保持任务的核心要素和执行细节。"""

TOOL_OUTPUT_EXECUTION_SUMMARY_PROMPT = """你是一个执行助手，需要总结工具执行结果。

当前执行步骤：{step_description}
工具输出内容：
{tool_output}

请总结关键信息：
1. 操作是否成功执行
2. 重要的输出结果和数据
3. 发现的问题或错误信息
4. 对后续步骤有用的信息
5. 文件路径、URL、关键数值等具体信息
6. 系统状态变化

要求：
- 保持简洁但不遗漏执行者需要的关键细节
- 突出与当前步骤相关的重要信息
- 如有错误要详细说明"""

CONTENT_SUMMARY_PROMPT = """请对以下内容生成简洁的摘要，保留关键信息：

上下文：{context}

内容：
{content}

请生成一个简洁但信息完整的摘要："""

USER_INTENT_EXTRACTION_PROMPT = """请分析以下用户输入，提取用户的核心意图和关键问题：

用户输入：
{user_input}

请提取：
1. 用户的主要目标或问题
2. 关键的要求或约束条件
3. 重要的上下文信息

格式要求：简洁明确，保留所有关键信息""" 